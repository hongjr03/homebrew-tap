# Homebrew Tap

## Install

```bash
brew install hongjr03/homebrew-tap/fusesoc
```

Or tap the repository first:

```bash
brew tap hongjr03/homebrew-tap
brew install fusesoc
```

Formulae are maintained in the `Formula/` directory. See [Homebrew's documentation](https://docs.brew.sh) for more information.
